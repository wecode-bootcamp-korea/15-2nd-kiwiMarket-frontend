import React from "react";
import { StyleSheet, Text, View, Button, Dimensions } from "react-native";

const TownInfo = () => <View style={{ backgroundColor: "#333", flex: 1 }} />;

export default TownInfo;
