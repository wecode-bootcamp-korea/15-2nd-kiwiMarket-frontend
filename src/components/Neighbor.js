import React from "react";
import { StyleSheet, Text, View, Button, Dimensions } from "react-native";

const Neighbor = () => <View style={{ backgroundColor: "#111", flex: 1 }} />;

export default Neighbor;
